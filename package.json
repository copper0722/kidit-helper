{
  "name": "kidit-helper",
  "version": "0.1.0",
  "private": true,
  "description": "A self-contained, nurse-first KiDit bookmarklet for searchable patient-form selects.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/static.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
