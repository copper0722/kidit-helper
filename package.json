{
  "name": "kidit-helper",
  "version": "0.1.5",
  "private": true,
  "description": "A self-contained KiDit bookmarklet for searchable transfer-hospital selection.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/static.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
